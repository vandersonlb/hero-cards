class Hero {
  constructor(id, name, url, bio, publisher) {
    this.id = id
    this.name = name
    this.img = [{ url }]
    this.bio = bio
    this.publisher = publisher
  }
}

export default Hero;
