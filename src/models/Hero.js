class Hero {
  constructor(name, url, bio, publisher, id) {
    this.name = name
    this.img = [{ url }]
    this.bio = bio
    this.publisher = publisher
    this.id = id
  }
}

export default Hero;
