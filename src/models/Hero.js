class Hero {
  constructor(name, image, bio, publisher, id) {
    this.name = name
    this.image = image
    this.bio = bio
    this.publisher = publisher
    this.id = id
  }
}

export default Hero;
